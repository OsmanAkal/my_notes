name: React CI + Docker

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Kodları al
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Node.js kur
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3️⃣ Bağımlılıkları yükle
      - name: Install dependencies
        run: npm install

      # 4️⃣ Testleri çalıştır
      - name: Run tests
        run: npm test -- --watchAll=false

      # 5️⃣ React projeyi build et
      - name: Build project
        run: npm run build

      # 6️⃣ Docker Hub login
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 7️⃣ Docker image oluştur
      - name: Build Docker image
        run: docker build -t osmanakal/my_notes:latest .

      # 8️⃣ Docker image'ı push et
      - name: Push Docker image
        run: docker push osmanakal/my_notes:latest

      # 9️⃣ Sunucuya deploy
      - name: Deploy to Server
        run: |
          ssh -o StrictHostKeyChecking=no user@YOUR_SERVER_IP '
            docker pull osmanakal/my_notes:latest &&
            docker stop my_notes || true &&
            docker rm my_notes || true &&
            docker run -d --name my_notes -p 80:80 osmanakal/my_notes:latest
          '
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
